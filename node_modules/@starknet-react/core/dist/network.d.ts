import { constants } from 'starknet';
/** Information about a Starknet network. */
export type Chain = {
    /** Chain ID. */
    id: string;
    /** Human-readable name. */
    name: string;
    /** Flag for test networks. */
    testnet?: boolean;
};
/** Returns a chain information from its id. */
export declare function chainById(chainId: constants.StarknetChainId): Chain | undefined;
